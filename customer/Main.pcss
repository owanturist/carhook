.main {
    & {
        display: flex;
        flex-direction: column;
        height: 100%;
    }

    &__logo {
        outline: none;
    }

    &__nav {
        z-index: 2;
        box-shadow: 0 0 10px rgba(0, 0, 0, .25);
    }
}
