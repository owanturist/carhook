html,
body {
    min-height: 100%;
    height: 100%;
    overflow: hidden;
}

.executor {
    &__logo {
        outline: none;
    }

    &__nav {
        z-index: 2;
        box-shadow: 0 0 10px rgba(0, 0, 0, .25);
    }
}
